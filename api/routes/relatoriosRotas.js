import express from "express"
import relatorioController from "../controllers/relatorioController.js"
const router = express.Router()

// router.get("/usuarios", relatorioController.listarUsuarios)
// router.post("/new", relatorioController.novo)
// router.patch("/relatorios", relatorioController.editar)
// router.delete("/relatorios", relatorioController.desativar)

export default router